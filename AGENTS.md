# AGENTS.md

コーディングエージェント向けの作業指示書です。人間向けの説明は [README.md](README.md) を参照してください。

## プロジェクト概要

Shift Match は、従業員の希望をもとに 1 日分のシフトを自動で組み立てる Web アプリケーションです。

- 操作者は管理者のみです。従業員ごとに勤務できる時間帯（開始・終了。「休み」も指定可）を入力すると、業務ルールをすべて満たし、入力した時間帯と割り当てた枠のずれが最も小さい割り当て案を 1 つ提案します
- 入力から結果表示まで 1 画面・1 リクエストで完結します
- 条件を満たす案が存在しない場合は、その旨を表示します

## 技術スタック

| 項目 | 内容 |
| --- | --- |
| 言語 | Java 25 |
| フレームワーク | Spring Boot 4.1.1 |
| ビルド | Maven（同梱の Maven Wrapper を使用） |

個々の依存ライブラリは `pom.xml` を参照してください。

- 画面は Thymeleaf + 素の JavaScript で構成します。フロントエンドフレームワークは導入しません
- データベースを使用しないため、`spring-boot-starter-data-jpa` や H2 などの依存は追加しません
- 状態を保持しない設計です。セッションやアプリケーションスコープにデータを持たせないでください
- 新しい依存を追加する場合は、事前にユーザーへ確認してください
- フレームワークの API の使い方に迷ったら、学習データの知識で推測せず、上記バージョンに対応する公式ドキュメントを確認してください

## ディレクトリ構成

```text
shift-match/
├── .agents/      # プロジェクトルール（コーディング規約・テスト規約など）
├── .github/      # Issue テンプレート・PR テンプレート
├── docs/         # 要求定義・要件定義
├── AGENTS.md     # 本書
├── README.md     # 人間向けの概要・起動方法
├── pom.xml
└── src/
    ├── main/
    │   ├── java/com/example/shiftmatch/   # アプリケーションコード
    │   └── resources/
    │       ├── application.properties
    │       ├── templates/                 # Thymeleaf テンプレート
    │       └── static/                    # JavaScript・CSS
    └── test/java/com/example/shiftmatch/  # テストコード
```

`target/` と `HELP.md` は生成物のため、コミットしないでください。レビュー結果は `target/reviews/` に出力します。

## コマンド

Maven はシステムにインストールされたものではなく、必ず Maven Wrapper（`./mvnw`）を使用してください。

```bash
# アプリケーションの起動（http://localhost:8080）
./mvnw spring-boot:run

# 全テストの実行
./mvnw test

# 特定のテストクラスのみ実行
./mvnw test -Dtest=クラス名

# 特定のテストメソッドのみ実行
./mvnw test -Dtest=クラス名#メソッド名

# ビルド（テストを含む）
./mvnw clean package

# Checkstyle のみを実行（./mvnw test にも validate フェーズで自動的に含まれる）
./mvnw checkstyle:check

# Spotless（google-java-format）でコードを自動整形（./mvnw test の前に実行する）
./mvnw spotless:apply

# Spotless の整形チェックのみを実行（./mvnw test にも validate フェーズで自動的に含まれる）
./mvnw spotless:check
```

## 信頼できる情報源

仕様の根拠は次のドキュメントです。実装・テストの前に必ず該当箇所を読んでください。

| ドキュメント | 内容 | 参照する場面 |
| --- | --- | --- |
| [docs/requirements.md](docs/requirements.md) | 要求定義（なぜ作るか・制約 C-x・スコープ・成功基準 S-x・用語） | 機能追加や変更の妥当性を判断するとき |
| [docs/specifications.md](docs/specifications.md) | 要件定義（機能 F-x・入力チェック V-x・制約 H-x・画面・非機能） | 実装・テストを書くとき |

- コードとドキュメントが食い違う場合は、ドキュメントを正とします。仕様自体を変える必要があると判断した場合は、実装せずにユーザーへ確認してください
- コミット・PR・テスト名などで仕様に言及するときは、ドキュメント内の ID（例：`V-2`、`H-4`）を使ってください
- 用語は要求定義 8 章に従います（「枠」「希望」「時間帯」）

## プロジェクトルール

コーディング規約・テスト規約などのプロジェクトルールは `.agents/rules/` に配置しています。コードを書く前に、作業内容に関係するルールファイルを読んでください。

## スコープ外（実装しないこと）

要求定義 6 章で対象外とされている機能は、依頼がない限り実装しません。

- 認証・ユーザー管理
- データの保存（データベース、ファイル、セッションへの保持を含む）
- 週次・月次のシフト作成
- 給与計算・勤怠実績の管理
- 出勤回数の公平化

## 作業の進め方

1. 依頼内容に対応する仕様（ID）をドキュメントで特定します
2. 仕様が曖昧・未決の場合は、実装前にユーザーへ確認します
3. 実装とテストを行い、「コマンド」セクションのコマンドでビルドとテストが通ることを確認します
4. PR を作成する場合は `.github/pull_request_template.md` の項目を埋めます

## レビュー結果の扱い

`code_quality_review` と `security_risk_review` は、最終返答として MUST の件数だけを返します。メインエージェントは件数だけで判断せず、レビュー後にそれぞれ `target/reviews/code-quality-review.md` と `target/reviews/security-risk-review.md` を読み、指摘の根拠と改修方針を確認して修正要否を判断してください。ファイルが存在しない場合や今回の結果か確認できない場合は、レビュー完了とみなさないでください。

作業完了を報告する際は、実行したコマンドとその結果（成功・失敗）を正確に伝えてください。

## ドメインの要点

詳細は要件定義を参照してください。ここでは、誤りやすく影響の大きい点のみを挙げます。

### 割り当て枠（固定）

| 枠 | 勤務時間 | 人数 | 休憩 |
| --- | --- | --- | --- |
| 1 | 7:30 〜 14:30 | 2 名 | 45 分 |
| 2 | 8:00 〜 15:30 | 1 名 | 45 分 |
| 3 | 8:30 〜 16:30 | 1 名 | 45 分 |
| 4 | 9:00 〜 16:30 | 1 名 | 45 分 |
| 5 | 9:00 〜 18:00 | 1 名 | 60 分 |
| 6 | 9:00 〜 18:30 | 2 名 | 60 分 |

この構成は勤務中人数（休憩中を含む。要件定義 2 章）と「早く出勤した人から順に退勤する」規則から導出されています。枠の追加や人数の可変化は、前提の変更にあたるため独断で行わないでください。休憩は労働基準法第 34 条に基づき、要件定義 2 章の規則で自動割り当てします。従業員の区分（正社員・役員・パート）は次の仕様変更で扱うため、今回は全員を同一に扱います。

### 算出ロジック

- **ハード制約（H-1〜H-3）**：枠 1 と枠 6 に 2 名ずつ、枠 2〜5 に 1 名ずつ、1 人 1 枠まで。枠の勤務時間が入力した時間帯に完全に含まれる（枠の開始 ≧ 入力の開始 かつ 枠の終了 ≦ 入力の終了）ときだけ割り当てる。「休み」の人は割り当てない
- **スコア（ずれの合計）**：割り当てられた 8 名それぞれの「入力した時間帯の長さ − 割り当てられた枠の勤務時間」（分）の合計。最小の案を採用します（0 が最良）
- **探索**：従業員の集合をビットマスクで表す動的計画法で、最小スコアの案を求めます（Issue #20 のレビューで、総当たりから変更。CPU 負荷を抑えるため）。最適化ライブラリは導入しません
- **同点時**：枠 1 → 6 の順に、入力順インデックスの辞書順で列挙したとき最初に最小スコアへ到達する案を採用します。動的計画法の復元でも、辞書順で最初に「最小スコアを保てる組」を選ぶことで同じ案を得ます。**同点で後の案に更新しません**（`<` で更新し、`<=` にしない）
- **不成立判定**：ハード制約を満たす案が 0 件であることで判定します。枠ごとの可能人数を独立に数える方法は誤判定するため使用しません（要件定義 6 章）

### 入力チェック

- サーバー側で V-1 → V-5 の順に行います
- 従業員名が空の行は、エラーにせず処理対象から除外します（V-1）
- 有効な行のうち「休み」でない行で、開始・終了が未選択・選択肢以外の値（7:30〜18:30 の 30 分単位以外）、または開始が終了以上の場合はエラーです（V-3）
- 有効な従業員のうち「休み」でない人が 8 名未満の場合は、エラーではなく不成立として扱います（V-4）
- 有効な従業員（休みを含む）が 13 名以上の場合はエラーです（V-5）

### 画面とフォームバインディング

- 入力行は素の JavaScript で動的に追加し、`name` 属性に連番インデックスを付与します（例：`employees[0].name`、`employees[0].off`、`employees[0].start`、`employees[0].end`）
- **インデックスに欠番があると Spring MVC で正しくバインドできません。** 行削除を実装する場合は、削除後に必ずインデックスを振り直してください

## 未決事項（TBD）

現在、未決事項はありません（T-1〜T-3 は決定済みで、決定内容は要件定義 2 章・6 章・7 章にあります）。要件定義 10 章に新たな未決事項が追加された場合、それに関わる実装は推測で決めずにユーザーへ確認してください。
