# Todo: 割当結果の表を「勤務時間・氏名・休憩」の 3 列にする

- Issue: #16
- ブランチ: feature/16-working-hours-display
- 版: v1
- 対象仕様: F-4, 7 章 出力仕様
- 作成日: 2026-09-24

## 前提

- 読むべきドキュメント・規約：`docs/specifications.md`（2 章の枠表、7 章 出力仕様）、`.claude/rules/tdd.md`、`.agents/rules/test.md`・`formatting.md`・`comment.md`
- 現状：割当結果の表は「枠（早番/遅番）・氏名・勤務時間（`8:00〜17:00`）・休憩」の 4 列（`src/main/resources/templates/index.html` の割当結果の表）
- 変更後：「勤務時間・氏名・休憩」の 3 列。「枠」列と「早番」「遅番」の文字は表から無くす。勤務時間は 24 時間表記・前 0 埋めで、早番の行が `08:00〜17:00`、遅番の行が `12:00〜21:00`（区切りは全角の `〜`）。休憩の表示は変更しない
- 設計方針：
  - `docs/` は変更しない（メインエージェントが更新済み）。サーバー側（Controller・Service・domain）は変更しない。新しい依存は追加しない。`pom.xml` は変更しない
  - 変更するのは `index.html` の割当結果の表と `ShiftControllerTest`（`src/test/java/com/example/shiftmatch/controller/ShiftControllerTest.java`）のみ
  - 早番/遅番の判定は既存と同じ `stat.index < 2`（先頭 2 行が早番、残りが遅番）を使う
  - テストの書き方は既存に合わせる（`@WebMvcTest` + `MockMvc`、`@MockitoBean`、本文検証は `getContentAsString()` + `Assertions.assertTrue`。Hamcrest は使わない）
  - 既存テストで `"早番"`・`"遅番"`・`8:00〜17:00`・列順「枠・氏名・勤務時間・休憩」を前提にしているものは 3 件ある（`[T-1][F-4]` 休憩時刻、`[F-4]` 表ヘッダの列順、`[F-4]` 早番/遅番の勤務時間）。新仕様に合わせて期待値を更新する。仕様に反する期待値へは変えない
  - 入力画面の希望選択などにある「早番」「遅番」（列見出し）は入力欄の表示なので変更しない。対象は割当結果の表だけ

## Todo

- [ ] **T1. 割当結果の表ヘッダが「勤務時間」・「氏名」・「休憩」の順で出力される（TDD）**
  - 依頼事項：`ShiftControllerTest` の `[F-4]` 表ヘッダの列順テスト（DisplayName に「枠」・「氏名」・「勤務時間」・「休憩」の順とあるもの）を、期待する順序を「勤務時間」・「氏名」・「休憩」に書き換え、DisplayName も合わせる。先にテストだけ変更して RED（アサーション失敗）を確認する。その後 `index.html` の `<th>` を「勤務時間」・「氏名」・「休憩」の 3 つにして GREEN にする。この時点では `<td>` も 3 列に揃える（先頭の `<td>` は枠の代わりに勤務時間を入れるので、T2 のために暫定でよいが、列数は 3 に揃える）。表ヘッダに「枠」が含まれないことも検証する
  - 対象ファイル：`src/test/java/com/example/shiftmatch/controller/ShiftControllerTest.java`、`src/main/resources/templates/index.html`
  - 完了条件：
    - テストを先に変更し、アサーションで失敗（RED）することを確認した
    - 表ヘッダが `勤務時間`・`氏名`・`休憩` の順であり、`枠` を含まないことを検証するテストが存在し成功する
    - `./mvnw test -Dtest=ShiftControllerTest` の結果を確認した（T2・T3 で更新する予定のテストは、この時点で失敗していてもよい）
- [ ] **T2. 早番の行に 08:00〜17:00、遅番の行に 12:00〜21:00 が先頭列に表示され、「早番」「遅番」は表示されない（TDD）**
  - 依頼事項：`ShiftControllerTest` の `[F-4]` 勤務時間テスト（DisplayName に「8:00〜17:00」「12:00〜21:00」とあるもの）を更新する。行 0・1（早番）は `08:00〜17:00`、行 2・3（遅番）は `12:00〜21:00` を同じ `<tr>` 内に氏名とともに含むことを検証し、`早番`・`遅番` を含まないことも検証する。先にテストだけ変更して RED を確認する。その後 `index.html` の先頭の `<td>` を `${stat.index < 2 ? '08:00〜17:00' : '12:00〜21:00'}` にし、氏名 → 休憩の順に並べて GREEN にする。旧来の勤務時間の `<td>`（氏名と休憩の間）が残っていれば削除する
  - 対象ファイル：`src/test/java/com/example/shiftmatch/controller/ShiftControllerTest.java`、`src/main/resources/templates/index.html`
  - 完了条件：
    - テストを先に変更し、アサーションで失敗（RED）することを確認した
    - 早番 2 行に `08:00〜17:00`、遅番 2 行に `12:00〜21:00` が出力され、`8:00〜17:00`（0 埋めなし）が出力されないことを検証するテストが存在し成功する
    - 割当結果の表の各行に `早番`・`遅番` の文字が出力されないことを検証するテストが存在し成功する
    - `./mvnw test -Dtest=ShiftControllerTest` の結果を確認した（T3 で更新する予定のテストは、この時点で失敗していてもよい）
- [ ] **T3. 休憩時刻の表示が変更されていない（既存テストの更新）**
  - 依頼事項：`ShiftControllerTest` の `[T-1][F-4]` 休憩時刻テストは、各行を「早番・太郎・13:00〜14:00」のように `早番`/`遅番` の文字で判定している。これを `早番`/`遅番` ではなく勤務時間（`08:00〜17:00` / `12:00〜21:00`）で行を特定する形に書き換え、氏名と休憩時刻（`13:00〜14:00` など、期待値は現状のまま）が同じ `<tr>` に含まれることを検証する。エラーメッセージ文言も合わせる。休憩の期待値は変更しない。`index.html` は原則変更しない（休憩の表示は現状どおり）
  - 対象ファイル：`src/test/java/com/example/shiftmatch/controller/ShiftControllerTest.java`
  - 完了条件：
    - 休憩時刻の期待値（`13:00〜14:00`、`14:00〜15:00`、`15:00〜16:00`、`16:00〜17:00`）は変更されていない
    - テスト内に、割当結果の表の行を `早番`/`遅番` の文字で判定している箇所が残っていない
    - `./mvnw test -Dtest=ShiftControllerTest` が成功する
- [ ] **T4. 全テストと静的解析が成功する**
  - 依頼事項：`./mvnw spotless:apply` で整形してから `./mvnw test` を実行し、失敗があれば修正する
  - 対象ファイル：T1〜T3 で変更したファイル
  - 完了条件：
    - `./mvnw test` で全テストが成功する（Spotless・Checkstyle の違反 0 件）

## 実行ログ

<!-- implementer が試行結果（失敗理由・リトライ回数）を追記する欄。作成時は空のままにする -->
